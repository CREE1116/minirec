# TurboCF Model Configuration
model_name: "turbocf"

model:
  reg_lambda: 100.0  # Ridge regularization (used for Chebyshev approximation)
  K: 3               # Polynomial degree
  alpha: 0.5         # Symmetric normalization strength
